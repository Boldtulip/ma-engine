[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ma-engine"
version = "0.1.0"
description = "An open-source M&A origination engine for China. It finds private companies whose owners are likely to sell, scores them, and drafts the first deal documents."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "MA Engine contributors" }]
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
ashare = ["requests>=2.31"]
llm = ["anthropic>=0.40"]
dev = ["pytest>=8.0"]

[project.scripts]
ma-engine = "ma_engine.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
